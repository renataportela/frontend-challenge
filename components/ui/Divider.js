import styled from '@emotion/styled'

import { colors, spacing } from '/tokens'

const Divider = styled.div`
  padding-top: ${spacing.t3};
  border-bottom: 1px solid ${colors.grey300};
  margin-bottom: ${spacing.t3};
  width: 100%;
`

export default Divider
